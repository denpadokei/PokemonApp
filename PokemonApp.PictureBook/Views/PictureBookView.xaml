<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="PokemonApp.PictureBook.Views.PictureBookView"             
             prism:ViewModelLocator.AutoWireViewModel="True" d:DesignWidth="1200" d:DesignHeight="800" Background="WhiteSmoke">
    <prism:Dialog.WindowStyle>
        <Style TargetType="{x:Type Window}">
            <Setter Property="Height" Value="800"/>
            <Setter Property="Width" Value="1200"/>
        </Style>
    </prism:Dialog.WindowStyle>
    <ContentControl>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" VerticalAlignment="Center">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <DockPanel Width="120" HorizontalAlignment="Left">
                        <TextBlock Text="名前"/>
                        <TextBox x:Name="NameBox" Text="{Binding Filter.PokemonName}" Height="20" Width="80"/>
                    </DockPanel>
                    <Button Grid.Column="1" Command="{Binding FilteringCommand}" Content="検索" Width="75" Height="30" HorizontalAlignment="Right"/>
                </Grid>
            </StackPanel>
            <ContentControl Grid.Row="1">
                <DataGrid ItemsSource="{Binding Pokemons}" AutoGenerateColumns="True"/>
            </ContentControl>
        </Grid>
    </ContentControl>
</UserControl>
